# Isolation rules

- Agents stay separated by default.
- Shared layer contains only genuinely cross-agent material.
- Another local head is off-limits unless the task explicitly requires handoff, migration, comparison, or review.
- Promotion to shared is deliberate, never silent.
